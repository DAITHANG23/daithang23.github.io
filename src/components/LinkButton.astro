---
export interface Props {
  id?: string;
  href: string;
  class?: string;
  ariaLabel?: string;
  title?: string;
  disabled?: boolean;
}

const { id, href, class: className = "", ariaLabel, title, disabled = false } = Astro.props;
---

{
  disabled ? (
    <span
      id={id}
      class:list={["group inline-block p-2 text-center", className]}
      title={title}
      aria-disabled={disabled}
    >
      <slot />
    </span>
  ) : (
    <a
      id={id}
      {href}
      class:list={[
        "group hover:text-primary text-primary-text inline-block transform rounded-full border-0 p-2 text-center",
        className,
      ]}
      aria-label={ariaLabel}
      title={title}
    >
      <slot />
    </a>
  )
}
